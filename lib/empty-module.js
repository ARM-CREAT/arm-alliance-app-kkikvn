// Empty stub for Node.js built-ins that don't exist in the React Native / web bundle.
// Metro resolves these via extraNodeModules in metro.config.js so that packages
// importing Node.js internals (crypto, stream, fs, etc.) don't crash the bundler.
module.exports = {};
